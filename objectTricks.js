//  Important object tricks - Modern JavaScript Syntaxes
const obj = {
    name: "javascript",
    founder: "Brendan Eich",
    estd: "1996",
    ranking: 1,
}
// var keys = Object.keys(obj);

// var value = Object.values(obj);

var entries = Object.entries(obj);

// console.log(keys);

// console.log(value);

console.log(entries);